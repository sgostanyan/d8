<?php

/**
 * Implements hook_preprocess_html().
 */
function d8_service_decorator_preprocess_html(&$variables) {
  $messageService = Drupal::service('d8_service_decorator.message.service');
  $message = $messageService->getMessage();
  $messageService->display($message);
}
